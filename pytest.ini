[pytest]
# Import ingester.py / sr.py from the repo root. Without this, bare `pytest` fails
# where `python -m pytest` works, because only the latter puts CWD on sys.path.
pythonpath = .
testpaths = tests
